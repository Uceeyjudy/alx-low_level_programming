#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 18; i < 28; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
